#!/usr/bin/env bash

if [ "$(( RANDOM % 67 ))" -eq 0 ]; then
  exec /usr/bin/fastfetch --logo /usr/share/zirconium/legally-distinct.txt "${@}"
else
  exec /usr/bin/fastfetch --logo /usr/share/zirconium/zirconium.txt "${@}"
fi
