---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: systemd
    system:
      enabled:
        - ublue-system-setup.service
        - chronyd.service
        - zirconium-time-setup.service
    user:
      enabled:
        - podman.socket
        - podman-restart.service
        - ublue-user-setup.service
